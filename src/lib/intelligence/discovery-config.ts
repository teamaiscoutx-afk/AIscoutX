/** Target number of live opportunities per niche refresh. */
export const DISCOVERY_IDEA_TARGET = 10;

/** Parallel Tavily + OpenAI calls per chunk (rate-limit safe). */
export const DISCOVERY_CONCURRENCY = 3;
